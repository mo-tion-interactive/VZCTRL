<Window x:Class="Ventuz.Views.Editor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ventuz.Views"
        xmlns:vm="clr-namespace:Ventuz.ViewModels"
        mc:Ignorable="d"
        Title="Editor"
        Height="450" Width="800"
        MinHeight="400" MinWidth="600"
        Icon="pack://application:,,,/Assets/CTRL.ico">

    <Window.Resources>
        <local:StringToBoolConverter x:Key="StringToBoolConverter"/>

        <DataTemplate x:Key="ButtonTemplate">
            <TextBlock Text="(kein Wert)" Foreground="Gray" />
        </DataTemplate>

        <DataTemplate x:Key="SliderTemplate">
            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
        </DataTemplate>

        <DataTemplate x:Key="SwitchTemplate">
            <CheckBox IsChecked="{Binding Value, Mode=TwoWay, Converter={StaticResource StringToBoolConverter}}" />
        </DataTemplate>

        <DataTemplate x:Key="LabelTemplate">
            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
        </DataTemplate>

        <local:ControlTypeTemplateSelector
            x:Key="ControlTypeTemplateSelector"
            ButtonTemplate="{StaticResource ButtonTemplate}"
            SliderTemplate="{StaticResource SliderTemplate}"
            SwitchTemplate="{StaticResource SwitchTemplate}"
            LabelTemplate="{StaticResource LabelTemplate}" />
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Ziel-IP:" VerticalAlignment="Center"/>
            <TextBox Width="200" Margin="10 0" Text="{Binding TargetIP, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Connect" Click="Connect_Click" Margin="10 0"/>
            <TextBlock Text="{Binding ConnectionStatus}" VerticalAlignment="Center" Margin="10 0" Foreground="Green"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Controls}" SelectedItem="{Binding SelectedControl}" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                    <GridViewColumn Header="Typ" DisplayMemberBinding="{Binding Type}" Width="100"/>
                    <GridViewColumn Header="Command" DisplayMemberBinding="{Binding Command}" Width="200"/>
                    <GridViewColumn Header="Wert" Width="150">
                        <GridViewColumn.CellTemplateSelector>
                            <StaticResource ResourceKey="ControlTypeTemplateSelector"/>
                        </GridViewColumn.CellTemplateSelector>
                    </GridViewColumn>
                    <GridViewColumn Header="Aktion">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Senden" Click="SendControl_Click"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Grid Grid.Row="2" Margin="0 10 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{Binding DebugOutput}"
                       Foreground="Gray"
                       FontSize="12"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       TextWrapping="Wrap"
                       Margin="0,0,10,0"/>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Hinzufügen" Click="Add_Click" Margin="5"/>
                <Button Content="Bearbeiten" Click="Edit_Click" Margin="5"/>
                <Button Content="Löschen" Click="Delete_Click" Margin="5"/>
                <Button Content="Speichern" Click="Save_Click" Margin="5"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="3" Visibility="Collapsed"/>
    </Grid>
</Window>